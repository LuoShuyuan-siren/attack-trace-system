

~~~text

#  API 接口规范

## 1. 文档说明

本文档定义 `attack-trace-system` 前后端及各分析模块之间的统一接口规范。

系统围绕以下三类安全数据进行统一处理：

- 主机日志：Windows / Linux 日志
- 主机行为：进程、文件、系统调用、网络行为等
- 网络流量：PCAP、Zeek、Suricata 等

所有原始数据经过 Parser 转换为统一事件 `NormalizedEvent`，再交由 Analyzer 产生统一检测结果 `DetectionResult`，最终用于 ATT&CK 映射、攻击关系图构建和攻击链溯源。

课程设计要求系统对主机日志、主机行为和网络流量进行多源采集与融合，并进一步构建攻击链和攻击关系图，因此项目中的数据接口必须保持统一。:contentReference[oaicite:0]{index=0}

---

# 2. API 基本信息

## 2.1 Base URL

本地开发环境：

```text
http://127.0.0.1:8000
~~~

API 统一版本前缀：

```text
/api/v1
```

示例：

```text
GET http://127.0.0.1:8000/api/v1/events/
```

------

## 2.2 API 文档

FastAPI Swagger：

```text
http://127.0.0.1:8000/docs
```

OpenAPI JSON：

```text
http://127.0.0.1:8000/openapi.json
```

------

## 2.3 数据格式

请求和响应统一使用：

```text
Content-Type: application/json
```

JSON 字段统一采用：

```text
snake_case
```

正确：

```json
{
  "event_id": "evt-001",
  "source_type": "host_log"
}
```

禁止自行使用：

```json
{
  "eventId": "evt-001",
  "sourceType": "host_log"
}
```

------

# 3. 时间格式规范

所有时间统一使用 ISO 8601。

推荐：

```text
2026-09-08T10:20:00Z
```

其中：

```text
Z
```

表示 UTC 时间。

示例：

```json
{
  "timestamp": "2026-09-08T10:20:00Z"
}
```

各 Parser 在解析原始日志时，应尽可能完成时间标准化。

------

# 4. ID 命名规范

## 4.1 Event ID

格式：

```text
evt-<UUID>
```

示例：

```text
evt-59b05ee9-eea6-422b-a6ad-fc5f43e8ec74
```

------

## 4.2 Detection ID

格式：

```text
det-<UUID>
```

示例：

```text
det-781ac7a7-c31f-4e50-92d3-f49e04ca2d20
```

------

## 4.3 Entity ID

### Host

```text
host:<hostname>
```

例如：

```text
host:WIN-PC01
```

### User

```text
user:<hostname>:<username>
```

例如：

```text
user:WIN-PC01:administrator
```

### Process

```text
process:<hostname>:<pid>
```

例如：

```text
process:WIN-PC01:3152
```

### IP

```text
ip:<ip-address>
```

例如：

```text
ip:192.168.1.20
```

### Domain

```text
domain:<domain>
```

例如：

```text
domain:evil.example.com
```

### File

```text
file:<hostname>:<path>
```

例如：

```text
file:WIN-PC01:C:\Users\admin\payload.exe
```

实体 ID 应尽量保持稳定。

同一个实体在不同事件中不得随意生成不同 ID。

------

# 5. 核心数据模型

# 5.1 NormalizedEvent

所有 Parser 最终必须输出：

```text
NormalizedEvent
```

对应代码：

```text
backend/app/schemas/event.py
```

基本结构：

```json
{
  "event_id": "evt-xxxxxxxx",
  "timestamp": "2026-09-08T10:20:00Z",

  "source_type": "host_log",
  "source": "windows_sysmon",

  "host": {
    "hostname": "WIN-PC01",
    "ip": "192.168.1.20",
    "os": "windows"
  },

  "event_type": "process_create",

  "subject": {
    "type": "process",
    "name": "powershell.exe",
    "pid": 3152,
    "user": "administrator"
  },

  "object": null,

  "network": null,

  "action": "create_process",

  "raw_data": {},

  "severity": "medium",

  "attack": null,

  "tags": [
    "powershell",
    "process"
  ]
}
```

------

## 5.2 source_type

只允许以下三个值：

| 值                | 含义                           |
| ----------------- | ------------------------------ |
| `host_log`        | Windows / Linux 主机日志       |
| `host_behavior`   | 主机进程、文件、系统调用等行为 |
| `network_traffic` | 网络流量                       |

禁止自行创建：

```text
windows
network
traffic
log
pcap
```

等新的一级类型。

具体来源通过：

```text
source
```

字段表示。

例如：

```text
windows_sysmon
windows_security
linux_auditd
linux_syslog
zeek
suricata
pcap
ebpf
```

------

## 5.3 event_type

表示具体事件类型。

推荐统一采用：

```text
小写 + 下划线
```

例如：

```text
process_create
process_terminate

user_login
user_logout

file_create
file_modify
file_delete
file_read

network_connection

dns_query

registry_modify
```

新增事件类型前，应尽量确认现有类型中不存在语义相同的定义。

例如不要同时出现：

```text
process_create
process_created
create_process
```

------

## 5.4 severity

统一使用：

| 值         | 含义 |
| ---------- | ---- |
| `info`     | 信息 |
| `low`      | 低   |
| `medium`   | 中   |
| `high`     | 高   |
| `critical` | 严重 |

禁止使用：

```text
warning
danger
error
important
```

------

# 6. Entity

对应：

```text
backend/app/schemas/entity.py
```

结构：

```json
{
  "entity_id": "process:WIN-PC01:3152",

  "entity_type": "process",

  "name": "powershell.exe",

  "host_id": "host:WIN-PC01",

  "first_seen": "2026-09-08T10:20:00Z",

  "last_seen": "2026-09-08T10:25:00Z",

  "attributes": {
    "pid": 3152,
    "ppid": 1140,
    "command_line": "powershell.exe -enc ..."
  },

  "tags": [
    "powershell"
  ]
}
```

当前支持的 Entity Type：

```text
host
user
process
file
ip
domain
registry
service
```

特殊字段统一存储于：

```text
attributes
```

禁止因为某一个模块需要额外字段，就直接修改公共 Entity 结构。

------

# 7. DetectionResult

所有 Analyzer 应统一返回：

```text
DetectionResult
```

对应：

```text
backend/app/schemas/detection.py
```

示例：

```json
{
  "detection_id": "det-xxxxxxxx",

  "timestamp": "2026-09-08T11:20:00Z",

  "analyzer": "dns_tunnel_analyzer",

  "detection_type": "malicious_behavior",

  "title": "Potential DNS Tunnel",

  "description": "检测到疑似 DNS 隧道通信行为",

  "severity": "high",

  "confidence": 0.91,

  "related_event_ids": [
    "evt-001",
    "evt-002",
    "evt-003"
  ],

  "related_entity_ids": [
    "host:PC01",
    "ip:192.168.1.20",
    "domain:example.com"
  ],

  "evidence": {
    "query_count": 328,
    "avg_domain_length": 57.2,
    "entropy": 4.73
  },

  "attack_technique_id": "T1071.004",

  "tags": [
    "dns",
    "tunnel"
  ]
}
```

------

## 7.1 detection_type

统一使用：

```text
anomaly
suspicious_behavior
malicious_behavior
policy_violation
```

------

## 7.2 confidence

范围：

```text
0.0 ~ 1.0
```

例如：

```text
0.95
```

表示检测算法具有较高置信度。

`confidence` 与 `severity` 含义不同：

```text
severity
=
攻击行为如果成立，其危害程度

confidence
=
分析器认为判断正确的概率/可信程度
```

------

# 8. AttackGraph

对应：

```text
backend/app/schemas/attack_graph.py
```

攻击关系图由：

```text
AttackNode[]
+
AttackEdge[]
```

组成。

------

## 8.1 AttackNode

示例：

```json
{
  "node_id": "host:WEB01",
  "node_type": "host",
  "name": "WEB01",
  "severity": "high",
  "attributes": {},
  "tags": []
}
```

原则：

```text
AttackNode.node_id
```

应尽量与：

```text
Entity.entity_id
```

保持一致。

------

## 8.2 AttackEdge

示例：

```json
{
  "edge_id": "edge-001",

  "source": "host:WEB01",

  "target": "host:PC01",

  "relation": "lateral_movement",

  "timestamp": "2026-09-08T10:35:00Z",

  "confidence": 0.88,

  "related_event_ids": [
    "evt-001",
    "evt-002"
  ],

  "related_detection_ids": [
    "det-001"
  ],

  "attack_technique_id": "T1021",

  "attributes": {}
}
```

------

## 8.3 relation

统一使用：

```text
小写 + 下划线
```

推荐关系：

```text
connect
spawn
execute
read
write
modify
login
authenticate
download
upload
resolve

initial_access
lateral_movement
privilege_escalation
c2_communication
data_exfiltration
```

当前 `relation` 为可扩展字符串。

新增关系前必须优先检查现有关系名称，避免同义词重复。

------

# 9. 当前 REST API

当前版本：

```text
v0.1
```

目前主要为项目骨架和前端并行开发提供 Mock / 占位接口。

------

# 9.1 系统状态

## GET /

功能：

```text
检查 API 服务是否运行
```

请求：

```http
GET /
```

响应：

```json
{
  "message": "Attack Trace System API",
  "status": "running"
}
```

------

# 9.2 健康检查

## GET /health

请求：

```http
GET /health
```

响应：

```json
{
  "status": "ok"
}
```

前端可以使用该接口检测后端连接状态。

------

# 10. Data API

接口前缀：

```text
/api/v1/data
```

## GET /api/v1/data/sources

获取后端当前支持的数据一级类型。

响应：

```json
{
  "source_types": [
    "host_log",
    "host_behavior",
    "network_traffic"
  ]
}
```

------

# 11. Events API

接口前缀：

```text
/api/v1/events
```

## GET /api/v1/events/

功能：

```text
查询标准化安全事件
```

当前版本响应：

```json
{
  "events": []
}
```

后续将增加：

```text
分页
时间范围筛选
source_type 筛选
host 筛选
severity 筛选
event_type 筛选
```

前端当前应按照：

```json
{
  "events": []
}
```

这一外层结构进行适配。

------

# 12. Attack API

接口前缀：

```text
/api/v1/attack
```

## GET /api/v1/attack/graph

功能：

```text
获取攻击关系图
```

当前响应：

```json
{
  "nodes": [],
  "edges": []
}
```

前端攻击图组件应以：

```text
nodes
edges
```

作为核心输入。

------

## GET /api/v1/attack/chain

功能：

```text
获取重建后的攻击链
```

当前响应：

```json
{
  "stages": []
}
```

后续可能返回：

```json
{
  "stages": [
    {
      "stage": "initial_access",
      "host": "WEB01",
      "technique_id": "T1190"
    },
    {
      "stage": "execution",
      "host": "WEB01",
      "technique_id": "T1059"
    },
    {
      "stage": "lateral_movement",
      "host": "PC01",
      "technique_id": "T1021"
    },
    {
      "stage": "command_and_control",
      "host": "PC01",
      "technique_id": "T1071"
    }
  ]
}
```

注意：

上述非空数据结构属于后续接口目标，目前后端尚未实现真实攻击链生成。

------

# 13. Tasks API

接口前缀：

```text
/api/v1/tasks
```

## GET /api/v1/tasks/

功能：

```text
获取分析任务列表
```

当前响应：

```json
{
  "tasks": []
}
```

后续计划支持：

```text
创建分析任务
查询任务状态
查看任务进度
查看任务结果
```

------

# 14. Parser 模块接口规范

所有原始数据解析器必须继承：

```python
BaseParser
```

位置：

```text
backend/app/core/parser.py
```

统一接口：

```python
def parse(
    self,
    source: Path,
) -> list[NormalizedEvent]:
    ...
```

因此所有 Parser 必须满足：

```text
输入：
原始文件或目录

输出：
list[NormalizedEvent]
```

典型流程：

```text
Sysmon.evtx
      ↓
WindowsSysmonParser
      ↓
NormalizedEvent[]
```

或者：

```text
capture.pcap
      ↓
TrafficParser
      ↓
NormalizedEvent[]
```

Parser 不负责最终判断攻击是否成立。

Parser 只负责：

```text
读取
解析
字段提取
格式标准化
```

------

# 15. Analyzer 模块接口规范

所有分析器必须继承：

```python
BaseAnalyzer
```

位置：

```text
backend/app/core/analyzer.py
```

统一接口：

```python
def analyze(
    self,
    events: list[NormalizedEvent],
) -> list[DetectionResult]:
    ...
```

因此所有 Analyzer 必须遵守：

```text
输入：
list[NormalizedEvent]

输出：
list[DetectionResult]
```

例如：

```text
NormalizedEvent[]
        ↓
DNSTunnelAnalyzer
        ↓
DetectionResult[]
```

禁止 Analyzer：

```text
直接返回字符串
直接返回任意自定义 dict
自行定义另一套 Event
改变公共 Schema
```

------

# 16. Parser 与 Analyzer 职责边界

统一数据处理流程：

```text
原始安全数据
      ↓
Parser
      ↓
NormalizedEvent
      ↓
Analyzer
      ↓
DetectionResult
      ↓
ATT&CK Mapping
      ↓
事件关联
      ↓
AttackGraph
      ↓
攻击链 / 溯源
```

例如：

```text
PCAP
 ↓
TrafficParser
 ↓
NormalizedEvent[]
 ↓
DNSTunnelAnalyzer
 ↓
DetectionResult
```

不要在 Parser 中直接完成攻击链判断。

不要在 Analyzer 中重新解析原始 PCAP / EVTX。

------

# 17. 前端开发规范

前端只通过：

```text
/api/v1/*
```

访问后端业务数据。

不要直接依赖：

```text
Python 内部对象
数据库结构
本地 JSON 文件路径
后端算法内部实现
```

前端 API 层建议统一封装，例如：

```text
src/api/
├── events.ts
├── attack.ts
├── tasks.ts
└── data.ts
```

前端展示组件不要直接硬编码后端 URL。

推荐统一配置：

```text
API_BASE_URL
```

开发环境：

```text
http://127.0.0.1:8000
```

------

# 18. 前端 Mock 数据规范

在真实算法模块完成前，前端允许使用 Mock 数据。

但 Mock 数据必须严格遵守本文档中的字段定义。

例如攻击图 Mock：

```json
{
  "nodes": [
    {
      "node_id": "host:WEB01",
      "node_type": "host",
      "name": "WEB01",
      "severity": "high",
      "attributes": {},
      "tags": []
    },
    {
      "node_id": "host:PC01",
      "node_type": "host",
      "name": "PC01",
      "severity": "medium",
      "attributes": {},
      "tags": []
    }
  ],

  "edges": [
    {
      "edge_id": "edge-001",
      "source": "host:WEB01",
      "target": "host:PC01",
      "relation": "lateral_movement",
      "confidence": 0.88,
      "related_event_ids": [],
      "related_detection_ids": [],
      "attack_technique_id": "T1021",
      "attributes": {}
    }
  ]
}
```

后续从 Mock 切换到真实接口时：

```text
数据结构不应发生改变
```

------

# 19. API 修改规则

公共 API 和 Schema 属于项目公共接口。

以下文件原则上由系统架构 / 接口负责人统一维护：

```text
backend/app/schemas/
backend/app/core/parser.py
backend/app/core/analyzer.py
backend/app/api/
docs/api.md
```

组员开发功能时，如发现现有接口无法满足需求：

```text
不要直接修改公共 Schema
```

应先提出 Issue 或在组内讨论：

```text
1. 当前需求是什么
2. 现有字段为什么无法满足
3. 希望新增什么字段
4. 是否影响其他模块
5. 是否影响前端
```

确定后统一修改接口。

------

# 20. 接口兼容原则

开发过程中优先：

```text
新增字段
```

避免：

```text
删除字段
重命名字段
修改已有字段含义
```

例如已经确定：

```text
source_type
```

不要随意修改成：

```text
source_category
```

已经确定：

```text
related_event_ids
```

不要修改成：

```text
events
```

否则会同时影响：

```text
Parser
Analyzer
Backend
Frontend
Tests
Mock Data
```

------

# 21. 当前接口状态

| 模块                   | 状态       |
| ---------------------- | ---------- |
| FastAPI 基础服务       | 已完成     |
| `/health`              | 已完成     |
| `/api/v1/data/sources` | 已完成     |
| `/api/v1/events/`      | 骨架完成   |
| `/api/v1/attack/graph` | 骨架完成   |
| `/api/v1/attack/chain` | 骨架完成   |
| `/api/v1/tasks/`       | 骨架完成   |
| `NormalizedEvent`      | 第一版完成 |
| `Entity`               | 第一版完成 |
| `DetectionResult`      | 第一版完成 |
| `AttackGraph`          | 第一版完成 |
| `BaseParser`           | 第一版完成 |
| `BaseAnalyzer`         | 第一版完成 |
| 数据上传 API           | 未实现     |
| 数据库                 | 未实现     |
| 真实 Event 查询        | 未实现     |
| 真实攻击图生成         | 未实现     |
| 真实攻击链生成         | 未实现     |
| 分析任务创建           | 未实现     |

------

# 22. 开发过程中最重要的约定

所有成员统一遵守：

```text
原始数据
    ↓
BaseParser
    ↓
NormalizedEvent
    ↓
BaseAnalyzer
    ↓
DetectionResult
    ↓
AttackGraph / Attack Chain
```

不要绕过统一数据接口直接让模块之间互相依赖。

接口存在的核心目的就是：

```text
不同成员可以独立开发
+
模块可以独立测试
+
最终能够统一集成
```

------

# 23. API Version

当前版本：

```text
API Version: v1
Project Version: 0.1.0
```

