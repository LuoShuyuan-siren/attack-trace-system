ADFA-LD 数据集简易使用指南
1. 数据集简介
ADFA-LD（ADFA Linux Dataset）是一个 Linux 主机入侵检测数据集，主要记录 Linux 系统中的 System Call
（系统调用）序列。
数据集规模较小，适合本项目进行前期开发、功能测试和算法验证。
本项目主要利用 ADFA-LD 验证：
• Linux 主机攻击行为分析；
• 攻击检测与 ATT&CK 映射；
• 攻击事件关联；
• 基础攻击图（AttackGraph）构建。
需要注意：ADFA-LD 主要是单台 Linux 主机上的 System Call 数据，并不天然提供多主机、多阶段攻击链。
2. 数据集目录
解压后通常可以看到：
ADFA-LD/
├── Training_Data_Master/
├── Validation_Data_Master/
└── Attack_Data_Master/
其中：
• Training_Data_Master/ ：正常行为训练数据；
• Validation_Data_Master/ ：正常行为验证数据；
• Attack_Data_Master/ ：攻击行为数据。
攻击数据通常进一步按照攻击类型划分，例如：
Attack_Data_Master/
├── Adduser/
├── Hydra-FTP/
├── Hydra-SSH/
├── Java-Meterpreter/
├── Meterpreter/
└── Webshell/
不同整理版本可能存在少量目录差异，以实际下载的数据为准。
1
3. 在本项目中的使用流程
ADFA-LD 可以用于完整验证一套攻击分析流程：
ADFA-LD 原始数据
 ↓
数据读取 / 预处理
 ↓
主机行为分析
 ↓
DetectionResult
 ↓
ATT&CK / TTP 映射
 ↓
攻击事件关联
 ↓
AttackEdge
 ↓
AttackGraph
 ↓
攻击链展示
因此，虽然 ADFA-LD 数据量很小，但足以帮助项目先把整条技术流程跑通。
4. 与本项目成员分工的对应关系
ADFA-LD 不需要所有成员直接处理，主要涉及以下成员：
成员 使用方式
成员3：Linux 主机日
志
了解 Linux 主机侧数据形式，辅助统一事件结构；不需要把 System Call 当作 
auth.log 等日志直接解析
成员4：主机行为分析 核心使用者；分析 System Call 序列并产生 DetectionResult
成员6：ATT&CK 与攻
击检测 根据检测出的攻击行为进行 ATT&CK Technique/Tactic 映射
成员7：攻击关联与溯
源
使用成员4、6产生的标准化检测结果，验证攻击事件关联和 AttackGraph 构建
成员9：靶场 / 数据集 /
测试 负责数据集保存、整理、测试数据划分和向其他成员提供测试数据
成员2（Windows 日志）和成员5（网络流量）不需要专门使用 ADFA-LD。
组长和前端成员主要使用处理后的统一结果，不需要直接处理原始 System Call 数据。
2
5. 攻击类型与项目中的使用
可以先选择少量攻击类型进行测试，例如：
Hydra-SSH
Webshell
Meterpreter
可将其用于验证不同类型的主机攻击行为：
ADFA-LD 类型 可关注的攻击行为
Hydra-SSH SSH 暴力破解 / 认证攻击
Hydra-FTP FTP 暴力破解 / 认证攻击
Webshell Web 入侵 / 命令执行
Meterpreter 远程控制 / 后渗透行为
Java-Meterpreter Java 环境下的后渗透行为
Adduser 用户创建 / 权限相关行为
具体 ATT&CK 映射由成员6根据项目规则确定，不要求数据集类别名称与 ATT&CK 技术名称完全一致。
6. 攻击关联与攻击链使用注意
这里需要特别注意：
ADFA-LD 中不同攻击类别的样本并不天然属于同一次攻击。
例如不能因为数据集中同时存在：
Hydra-SSH
Webshell
Meterpreter
就直接认为：
Hydra-SSH
 ↓
Webshell
 ↓
Meterpreter
是真实发生的一条攻击链。
3
正确的使用方式是：
ADFA-LD
 ↓
成员4：主机行为分析
 ↓
DetectionResult
 ↓
成员6：ATT&CK / TTP 映射
 ↓
攻击行为
 ↓
成员7：关联规则
 ↓
AttackEdge
 ↓
AttackGraph
这样可以完整验证你们系统从攻击数据到攻击图的处理流程。
但是最终报告中应表述为：
“基于 ADFA-LD 验证攻击行为检测、ATT&CK 映射和攻击图构建流程。”
而不要表述为：
“从 ADFA-LD 还原了一条真实完整的多阶段攻击链。”
真正的多主机、多阶段攻击链溯源，可以再使用 LANL 等具有跨主机、多源日志关联能力的数据集进行验证。
7. 第一轮建议测试
第一次不要把所有数据都处理掉，可以先选择：
正常数据
+
Hydra-SSH
+
Webshell
+
Meterpreter
先完成：
数据读取
 ↓
4
数据预处理
 ↓
攻击/正常识别
 ↓
攻击行为分析
 ↓
DetectionResult
 ↓
ATT&CK 映射
 ↓
AttackGraph
确认整个流程能够正常运行后，再增加其他攻击类型。
8. 数据存放建议
建议将原始数据单独保存：
attack-trace-system/
├── data/
│ ├── adfa-ld/
│ │ ├── Training_Data_Master/
│ │ ├── Validation_Data_Master/
│ │ └── Attack_Data_Master/
│ │
│ └── 其他数据集/
│
├── docs/
├── backend/
├── frontend/
└── ...
原始数据不要直接修改。
如果数据集不准备提交 GitHub，可以在 .gitignore 中加入：
data/
代码只保存数据读取、处理和分析逻辑。
9. 本项目中的定位
ADFA-LD 在本项目中的定位是：
小规模的 Linux 主机攻击数据集，用于低成本验证完整的攻击分析流程。
5
它可以帮助项目完成：
原始数据
 ↓
主机行为分析
 ↓
攻击检测
 ↓
ATT&CK 映射
 ↓
事件关联
 ↓
AttackGraph
因此，数据集虽然很小，但并不意味着只能做简单的分类实验。
只要将 ADFA-LD 的攻击样本经过成员4、成员6处理，再交给成员7进行关联，就可以把你们项目中的一整套攻
击分析流程跑下来。
它主要用于验证流程和功能；后续再用 LANL 等大型数据集验证真正的跨主机、多源攻击关联与溯源能力。